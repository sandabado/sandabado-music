"use client"

import { useState, type FormEvent } from "react"
export default function ContactPage() { const [status,setStatus] = useState<"idle"|"sending"|"done"|"error">("idle"); async function submit(event:FormEvent<HTMLFormElement>) { event.preventDefault(); setStatus("sending"); const form = new FormData(event.currentTarget); try { const res = await fetch("/api/contact", { method:"POST", headers:{ "Content-Type":"application/json" }, body:JSON.stringify(Object.fromEntries(form)) }); if (!res.ok) throw new Error(); setStatus("done"); event.currentTarget.reset() } catch { setStatus("error") } } return <div className="star-field min-h-screen px-6 py-16"><section className="mx-auto grid max-w-5xl gap-12 md:grid-cols-[.8fr_1.2fr]"><div><p className="text-[10px] uppercase tracking-[.28em] text-[var(--gold)]">Get in touch</p><h1 className="mt-3 font-[family-name:var(--font-display)] text-5xl">Contact</h1><div className="mt-7 space-y-5 text-sm leading-6 text-[var(--cream)]/65"><p><span className="block text-[var(--gold)]">Booking</span><a className="sand-link" href="mailto:booking@sandabado.com">booking@sandabado.com</a></p><p><span className="block text-[var(--gold)]">Press</span><a className="sand-link" href="mailto:press@sandabado.com">press@sandabado.com</a></p><p><span className="block text-[var(--gold)]">Mailing address</span>Whole Body Records<br/>c/o Jesse Gawlik<br/>Sheridan, WY</p></div></div><form onSubmit={submit} className="space-y-5 border border-white/15 bg-white/[.025] p-6 sm:p-8"><Field label="Name" name="name" required/><Field label="Email" name="email" type="email" required/><label className="block text-sm text-[var(--cream)]/75">Subject<select required name="subject" defaultValue="" className="mt-2 w-full border border-white/20 bg-[var(--obsidian)] px-3 py-3 text-[var(--cream)] outline-none focus:border-[var(--gold)]"><option value="" disabled>Select a subject</option><option>Booking</option><option>Press</option><option>General</option></select></label><label className="block text-sm text-[var(--cream)]/75">Message<textarea required name="message" rows={5} maxLength={5000} className="mt-2 w-full border border-white/20 bg-[var(--obsidian)] px-3 py-3 text-[var(--cream)] outline-none focus:border-[var(--gold)]"/></label><button disabled={status === "sending"} className="w-full bg-[var(--gold)] px-5 py-3 text-xs font-semibold uppercase tracking-[.14em] text-[var(--obsidian)] disabled:opacity-60">{status === "sending" ? "Sending…" : "Send message"}</button><p role="status" className="text-center text-sm text-[var(--cream)]/65">{status === "done" ? "Your message is on its way." : status === "error" ? "Couldn’t send right now. Please email us directly." : ""}</p></form></section></div> }
function Field({ label,name,type="text",required=false }: { label:string; name:string; type?:string; required?:boolean }) { return <label className="block text-sm text-[var(--cream)]/75">{label}<input required={required} name={name} type={type} maxLength={150} className="mt-2 w-full border border-white/20 bg-[var(--obsidian)] px-3 py-3 text-[var(--cream)] outline-none focus:border-[var(--gold)]"/></label> }
